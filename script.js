const rows=document.querySelectorAll('.rows');
const cost=document.querySelector('.cost');

let totalSeats=document.querySelector('.totalSeats');

let movie=document.getElementById('movie');

const seats=documentdocument.querySelectorAll('.seats:not(.occupied');

let removeSeats=false;
let seatRow=0;
let seatNumber=0;



if (Element.classList.value == 'seats') {
    Element.classList.add('selectedSeats');
    
    RemoveSeat = false;
} else {
    Element.classList.remove('selectedSeats');

    removeSeat=true;
}